/// Returns the total of all integers passed in. With no arguments, returns 0.
pub fn sum(nums: &[i32]) -> i32 {
    -1
}

/// Returns integer quotient `a / b`. When `b` is zero, return an error.
pub fn divide(a: i32, b: i32) -> Result<i32, String> {
    Ok(0)
}
