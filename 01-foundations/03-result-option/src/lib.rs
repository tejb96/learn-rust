/// Parses a port number from a string. Returns `None` for empty input or non-numeric text.
pub fn parse_port(s: &str) -> Option<u16> {
    None
}

/// Returns the first even number in the slice, or `None` if none exist.
pub fn first_even(nums: &[i32]) -> Option<i32> {
    None
}

/// Divides `a` by `b`, returning `Err` when `b` is zero.
pub fn safe_divide(a: f64, b: f64) -> Result<f64, String> {
    Ok(0.0)
}
