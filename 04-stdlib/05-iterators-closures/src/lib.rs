/// Returns squares of numbers greater than `threshold`.
pub fn squares_above(nums: &[i32], threshold: i32) -> Vec<i32> {
    vec![]
}

/// Sums lengths of strings after trimming whitespace.
pub fn total_trimmed_len(words: &[&str]) -> usize {
    0
}

/// Builds a closure that adds `n` to its input.
pub fn make_adder(n: i32) -> impl Fn(i32) -> i32 {
    move |x| x
}

/// Partitions numbers into (evens, odds) preserving order within each group.
pub fn partition_evens_odds(nums: &[i32]) -> (Vec<i32>, Vec<i32>) {
    (vec![], vec![])
}
