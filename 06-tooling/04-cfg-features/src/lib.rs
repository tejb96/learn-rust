/// Returns the active feature label.
pub fn feature_label() -> &'static str {
    "default"
}

/// Returns bonus value when `enabled` feature is active, otherwise 0.
pub fn bonus_value() -> i32 {
    0
}
