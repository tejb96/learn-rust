# Benchmarks

## What you'll learn

- Correctness tests vs performance benchmarks
- Criterion setup with `harness = false`
- Iterative vs recursive algorithms

Tests verify correctness. Optionally run `cargo bench -p benchmarks_lesson`.

## Before moving on

- [ ] All tests pass: `cargo test`
