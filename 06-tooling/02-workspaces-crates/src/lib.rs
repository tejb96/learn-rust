use calc_core::{add, mul};

/// Computes `(a + b) * c` using calc_core.
pub fn sum_then_mul(a: i32, b: i32, c: i32) -> i32 {
    0
}

/// Computes `a * b + c` using calc_core.
pub fn mul_then_add(a: i32, b: i32, c: i32) -> i32 {
    0
}
