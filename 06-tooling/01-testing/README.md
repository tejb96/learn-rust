# Testing

## What you'll learn

- Integration tests in `tests/`
- Reading test failures to find bugs
- Edge cases like `i32::MIN`

Fix bugs in `src/lib.rs` until all tests pass.

## Before moving on

- [ ] All tests pass: `cargo test`
