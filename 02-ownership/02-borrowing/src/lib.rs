/// Appends `suffix` to `s` in place.
pub fn append_in_place(s: &mut String, suffix: &str) {
}

/// Returns the longest string by length. Ties return the first.
pub fn longest<'a>(a: &'a str, b: &'a str) -> &'a str {
    a
}

/// Doubles every element in the slice in place.
pub fn double_in_place(nums: &mut [i32]) {
}
