# Borrowing

## What you'll learn

- Immutable references `&T` and mutable references `&mut T`
- Borrow checker rules (one mutable OR many immutable)
- Slices as borrowed views

## Before moving on

- [ ] All tests pass: `cargo test`
