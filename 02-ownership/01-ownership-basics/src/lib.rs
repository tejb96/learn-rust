/// Takes ownership of `s` and returns it uppercased.
pub fn shout(s: String) -> String {
    String::new()
}

/// Returns a new string with `suffix` appended. Takes ownership of both parts.
pub fn concat_owned(a: String, b: String) -> String {
    String::new()
}

/// Returns the length of `s` without taking ownership (uses borrowing internally).
pub fn len_copy(s: &str) -> usize {
    0
}
