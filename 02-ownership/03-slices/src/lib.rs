/// Returns a slice of `data` from `start` (inclusive) to `end` (exclusive).
/// Returns `None` if the range is out of bounds.
pub fn safe_slice(data: &[i32], start: usize, end: usize) -> Option<&[i32]> {
    None
}

/// Returns the substring before the first space, or the whole string if no space.
pub fn first_word(s: &str) -> &str {
    ""
}

/// Sums all elements in the slice.
pub fn sum_slice(nums: &[i32]) -> i32 {
    0
}
