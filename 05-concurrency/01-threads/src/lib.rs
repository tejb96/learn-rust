use std::thread;

/// Spawns a thread that doubles each element, then joins and returns the result.
pub fn parallel_double(nums: Vec<i32>) -> Vec<i32> {
    vec![]
}

/// Returns the sum of `nums` by splitting work across two threads.
pub fn parallel_sum(nums: Vec<i32>) -> i32 {
    0
}
